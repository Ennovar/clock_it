// import { fromJS } from 'immutable';
// import { makeSelectClockInPageDomain } from '../selectors';

// const selector = makeSelectClockInPageDomain();

describe('makeSelectClockInPageDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
